apiVersion: v1
kind: Service
metadata:
  name: qwen-nodeport
  namespace: inference
spec:
  type: NodePort
  selector:
    app: isvc.qwen-predictor
  ports:
    - name: http
      port: 8000
      targetPort: 8080
      nodePort: 30434

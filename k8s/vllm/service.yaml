apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: inference
spec:
  type: NodePort
  selector:
    app: vllm
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30434
